// -  Create a variable named `numbers` with the following content:
//   `[3, 4, 5, 6, 7]`
// -  Log the sum of the elements of `numbers` to the console

let numbers = [3, 4, 5, 6, 7];

let abc = numbers[0] + numbers[1] + numbers[2] + numbers[3] + numbers[4];

console.log(abc);
 

